### Utility that can be integrated with slstatus

1. Use `run_command` to run `fetch_time`. It will fetch time from a yml file.
2. Run `timer time` to write on yml file, so that `fetch_time` can display it.
